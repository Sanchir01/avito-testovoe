env: "local"

database:
  host: "0.0.0.0"
  port: "5432"
  user: "postgres"
  dbname: "postgres"
  max_attempts: 10

servers:
  http:
    port: "8080"
    host: "localhost"
    timeout: 4s
    idle_timeout: 60s